datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model WaterLog {
  id       Int    @id @default(autoincrement())
  userId   String
  date     String
  intakeMl Int

  @@unique([userId, date])
}
